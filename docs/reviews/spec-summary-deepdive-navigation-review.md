# Adversarial Spec Review — summary↔deep-dive navigation

**NOTE: Codex at usage limit (until Jul 18 2026); Claude (opus) adversarial review per docs/plugins.md fallback.**

Verdict: **needs-rework** → applied. Core design (shared `t=<sec>s` timeline join, client-side URL inheritance, counterpart-gating) is sound and move-safe; rework is a naming/signature error, a `start=0` truthiness trap, and a migration-cost correction.

## Applied

- **B1 — wrong function name + signature.** The summary renderer is `renderMagazineHtml(parsed: ParsedSummary, model: MagazineModel)` (render.ts:53), NOT `renderSummaryHtml(model, parsed, sourceMd)`. → Spec corrected everywhere; new param appended: `renderMagazineHtml(parsed, model, hasDeepDive = false)` (no invented `sourceMd`/reorder; sourceMd comes from `parsed.sourceMd`).
- **B2/B3 — `start=0` truthiness trap.** `data-start` must be gated on PRESENCE of the timeRange/ts object (`s.timeRange ? …` / `ts ? …`), NEVER a truthiness test on `startSec` (a `0:00` section has `startSec===0`, falsy → would drop the attribute and make the first section non-navigable). → Spec pins presence-based gating + adds explicit `start=0` test rows (renderer emits `data-start="0"`; jsdom scroll lands on it).
- **H1 — no double-parse.** `SectionTimeRange.startSec` is already a parsed integer (types.ts:5, parse.ts:33). → Summary uses `s.timeRange.startSec` directly; `startSecFromTsUrl` is **deep-dive-only** (renderSection has just `ts.url` string). Both feed `data-start`/`data-t` as NUMBERS → no injection vector.
- **H3 — "no Gemini" was false for summaries; RESOLVED by not bumping the summary version.** Only **24 of 260** summaries have a cached magazine model; `reRenderSummaryHtml` without a model → `skipped-no-model` → `ensureHtmlDoc` falls back to `runHtmlDoc` → **Gemini re-summarize**. A summary minor bump would lazily re-summarize **236 docs** on open. → **Decision: do NOT bump `CURRENT_DOC_VERSION`.** Nav matters only on the **18** videos with a deep-dive (the "dig deeper" link is counterpart-gated); of those, **13** summaries have a model (cheap targeted re-render) and **5** don't. Migration eagerly re-renders the 13 (no Gemini, no content change); the 5 no-model ones are left with **partial** nav (reachable from the deep-dive, but no in-page scroll / no "dig deeper" until naturally re-summarized) — documented, no surprise cost. Deep-dive side keeps the clean `{2,2}→{2,3}` bump (renders from `.md`, no model). NOTE (broader tech-debt, not this feature's job): summary minor bumps are inherently expensive because magazine models aren't persisted for old docs.
- **H2 — version blast radius (now SMALLER, summary not bumped).** Only the deep-dive bump breaks assertions: `tests/lib/deep-dive/version.test.ts:6` (`{2,2}→{2,3}`) and `tests/components/VideoMenu.test.tsx:34,51,67` (deepDiveVersion fixtures `{2,2}→{2,3}`, else the deep-dive link goes stale→`<button>`). UNAFFECTED (no summary bump): `doc-version.test.ts`, VideoMenu docVersion fixtures (:15,:27), `ensure.test.ts` `{3,3}` fixtures, `timestamp-audit.test.ts:21` (audit is major-only anyway).
- **M1** — only other `renderMagazineHtml`/`renderDeepDiveHtml` callers are tests (render*.test, darkmode-html.spec); all 4 production drivers have the `Video` in scope to thread the flag; default-`false` degrade is safe; tests exercise the no-nav default unless updated. Stated.
- **M2** — `new URL()` + `searchParams.set('type',…)` + `u.search` round-trips `outputFolder` correctly (no manual `encodeURIComponent`, so no double-encoding; `id` stays in `u.pathname`). jsdom test asserts the rebuilt href preserves `outputFolder` + `id`; note the `+`-in-path caveat (absolute mac paths have none; slugs shouldn't).
- **M3** — both `data-start` null branches pinned: ts/timeRange present → `data-start="N"`; absent → no attr (never `"null"`/`"NaN"`).
- **M4** — `NAV_SCRIPT` injected at **end-of-body** (alongside `THEME_TOGGLE_SCRIPT`), not `<head>` — it queries the DOM. No CSP/nonce (serve route sets only Content-Type; consistent with existing inline scripts).
- **L4 — testability.** Extract the wiring + scroll into named exported functions (`wireDigLinks(doc, loc)`, `scrollToHashSection(doc, loc)`) that `NAV_SCRIPT` calls; jsdom-unit-test those (with `/** @jest-environment jsdom */`) instead of eval'ing a script string.
- **L3** — fix the stale `ensure.test.ts:86` comment + update the version doc-comments.

## Verified-correct (reviewer)
`data-start` is NOT model-dependent — `reRenderSummaryHtml` re-parses `parsed` from the live `.md` (rerender.ts:49), the model only supplies lead/bullets; so the timeline survives re-render. Scroll guard `Math.max(...,-1)` + `target>=0` handles `start=0` correctly. Inline script injection matches existing pattern. `runDeepDiveHtml`'s `hasSummary` is reliably true (deep-dive generated from an existing summary).
